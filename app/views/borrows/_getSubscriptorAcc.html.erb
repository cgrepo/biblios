<%= bootstrap_form_tag  url: :findByTitle_borrows, :html => {id:'commitSearchBySubAcc'} do |f| %>
    <%= f.text_field_with_bootstrap :account, label:'CUENTA', id:'acct' %>
    <%= f.submit 'BUSCAR' %>
<% end %>
