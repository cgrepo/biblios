<div class='row'>
    <div class='col-xs col-md-12'>
        <%#= render :partial => 'books/books' %>
        <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>ID</th>
                  <th>ISBN</th>
                  <th>Titulo</th>
                  <th>Autor</th>
                  <th colspan="1"></th>
                </tr>
              </thead>
              <tbody>
                <% @books.each do |book| %>
                  <tr>
                    <td><%= book.id %></td>
                    <td><%= book.isbn %></td>
                    <td><%= book.title %></td>
                    <td><%= book.autor %></td>
                    <td>
                      <%= link_to book, class:'btn btn-md ' do %>
                        <%= octicon 'repo-push', class:'rigth' %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
      </div>
    </div>
</div>
